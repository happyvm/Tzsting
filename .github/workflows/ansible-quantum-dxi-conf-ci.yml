name: ansible-quantum-dxi-conf CI

on:
  push:
    paths:
      - 'ansible-quantum-dxi-conf/**'
      - '.github/workflows/ansible-quantum-dxi-conf-ci.yml'
  pull_request:
    paths:
      - 'ansible-quantum-dxi-conf/**'
      - '.github/workflows/ansible-quantum-dxi-conf-ci.yml'

defaults:
  run:
    working-directory: ansible-quantum-dxi-conf

jobs:
  ansible-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install tooling
        run: pip install yamllint ansible-core ansible-lint

      - name: Install required collections
        run: ansible-galaxy collection install -r requirements.yml

      - name: yamllint
        run: yamllint .

      - name: ansible-lint
        run: ansible-lint

      - name: ansible-playbook --syntax-check
        run: |
          cp inventory/hosts.yml.example inventory/hosts.yml
          ansible-playbook playbooks/configure_dxi.yml --syntax-check
