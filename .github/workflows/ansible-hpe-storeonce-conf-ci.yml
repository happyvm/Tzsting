name: ansible-hpe-storeonce-conf CI

on:
  push:
    paths:
      - 'ansible-hpe-storeonce-conf/**'
      - '.github/workflows/ansible-hpe-storeonce-conf-ci.yml'
  pull_request:
    paths:
      - 'ansible-hpe-storeonce-conf/**'
      - '.github/workflows/ansible-hpe-storeonce-conf-ci.yml'

defaults:
  run:
    working-directory: ansible-hpe-storeonce-conf

jobs:
  ansible-lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install tooling
        run: pip install yamllint ansible-core ansible-lint

      - name: Install required collections
        run: ansible-galaxy collection install -r requirements.yml

      - name: yamllint
        run: yamllint .

      - name: ansible-lint
        run: ansible-lint

      - name: ansible-playbook --syntax-check
        run: |
          cp inventory/hosts.yml.example inventory/hosts.yml
          ansible-playbook playbooks/configure_storeonce.yml --syntax-check
