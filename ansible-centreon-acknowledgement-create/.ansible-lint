---
profile: basic

skip_list:
  # centreon_ack_token (set in roles/auth) is read by roles/acknowledgement
  # in the same play, so this project deliberately uses a project-wide
  # `centreon_ack_` prefix instead of a per-role one.
  - var-naming[no-role-prefix]

exclude_paths:
  - inventory/hosts.yml
