---
collections:
  - name: ansible.windows
    version: ">=2.0.0,<4.0.0"
  - name: community.windows
    version: ">=2.0.0"

# No dedicated collection covers AD domain join or NTP client configuration
# on plain Windows Server (only win_user and win_timezone are real modules
# here) - see roles/ad and roles/ntp for the raw PowerShell fallback and why.
# community.windows.win_snmp (used by roles/snmp) is a real module, but
# only manages community strings and permitted managers - see roles/snmp
# and the README for the SNMPv3/trap destination limitations.
