---
# Copy to hosts.yml. The Hyper-V host is reached over WinRM from a dynamic
# in-memory inventory entry (hyperv_conf_target) added by the playbook
# itself from hyperv_conf_hostname/username/password - no static per-host
# inventory is required.
all:
  hosts:
    localhost:
      ansible_connection: local
