---
collections:
  - name: ansible.windows
# The ConfigurationManager PowerShell module itself is not an Ansible
# collection - it ships with the SCCM console and is invoked here via
# ansible.windows.win_powershell on a Windows host that already has it
# installed. See README.
