---
# Copy this file to hosts.yml and adapt it to your environment.
#
# There is deliberately no per-VM inventory here: the target VM, its
# credentials and the requested size are supplied as extra-vars at run
# time (typically by a ServiceNow workflow calling an AWX/Tower job
# template - see playbooks/resize_disk.yml for the full list of vars).
#
# This inventory only needs to describe the *hypervisor estate* itself:
# the fleet of Hyper-V hosts the playbook should search when looking for
# a given VM by name. The vCenter connection is a single set of
# credentials/endpoint, defined in group_vars/all.yml instead (a Windows
# VM's placement inside vCenter's inventory tree doesn't need its own
# host entry - the vCenter API is queried directly by VM name).

all:
  children:

    hyperv_hypervisor:
      hosts:
        hyperv-host01:
          ansible_host: hyperv-host01.example.local
          ansible_connection: winrm
          ansible_winrm_transport: kerberos
          ansible_port: 5986
          ansible_user: "{{ vault_hyperv_username }}"
          ansible_password: "{{ vault_hyperv_password }}"

        hyperv-host02:
          ansible_host: hyperv-host02.example.local
          ansible_connection: winrm
          ansible_winrm_transport: kerberos
          ansible_port: 5986
          ansible_user: "{{ vault_hyperv_username }}"
          ansible_password: "{{ vault_hyperv_password }}"
